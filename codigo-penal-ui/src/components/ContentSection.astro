---
const { id, title, subtitle } = Astro.props;
---
<section class="w-full max-w-prose" id={id}>
  <h2 class="mt-12 mb-8 title-2">{title}</h2>
  {subtitle ? <h3 class="mb-6 mt-10 title-3">{subtitle}</h3> : null}
  <slot />
</section>
